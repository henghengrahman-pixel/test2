<div class="top-strip top-strip-clean">
  <div class="wrap">
    <div class="strip-row">
      <div class="strip-left">Extrait de Parfum</div>
      <div class="strip-right">
        <span
          class="cartlink"
          onclick="window.MawarCart.toggle()"
          role="button"
          tabindex="0"
        >
          Cart <b id="cartCount">0</b>
        </span>

        <a href="/checkout" class="checkout-link" aria-label="Checkout">
          Checkout
        </a>
      </div>
    </div>
  </div>
</div>

<header class="header header-clean">
  <div class="wrap">
    <div class="row row-clean">
      <div class="head-actions mobile-left">
        <button class="iconbtn iconbtn-clean" id="mobileMenuBtn" type="button" aria-label="Open menu">☰</button>
      </div>

      <h1 class="brand brand-wrap" style="margin:0">
        <a href="/" class="brand-link brand-link-clean" aria-label="Mawar Parfume Home">
          <span class="brand-text-clean">MAWAR PARFUME</span>
        </a>
      </h1>

      <div class="head-actions">
        <button class="iconbtn iconbtn-clean" id="openSearchBtn" type="button" aria-label="Open search">⌕</button>
      </div>
    </div>
  </div>

  <div class="nav nav-clean" id="mainNav">
    <ul>
      <li><a href="/">Fragrance</a></li>
      <li><a href="/" data-filter="MIX" class="cat-link">Parfume Mix</a></li>
      <li><a href="/" data-filter="MEN" class="cat-link">Men</a></li>
      <li><a href="/" data-filter="WOMEN" class="cat-link">Women</a></li>
      <li><a href="/" data-filter="UNISEX" class="cat-link">Unisex</a></li>
      <li><a href="/admin">Admin</a></li>
    </ul>
  </div>
</header>

<div class="search-overlay search-overlay-clean" id="searchOverlay" aria-hidden="true">
  <div class="searchbar searchbar-clean">
    <div class="searchhint">Search fragrance</div>
    <div class="searchrow searchrow-clean">
      <span class="sicon">⌕</span>
      <input id="q" placeholder="Search perfume..." autocomplete="off" inputmode="search">
    </div>
    <button class="closebtn closebtn-clean" id="closeSearchBtn" type="button">Close</button>
  </div>
</div>

<div id="cartDrawer" class="cartDrawerClean" aria-hidden="true" style="position:fixed;inset:0;z-index:2000;display:none;">
  <div class="cart-backdrop" onclick="window.MawarCart.toggle(false)"></div>

  <aside class="cart-panel">
    <div class="cart-panel-head">
      <h3>My Cart</h3>
      <button class="iconbtn iconbtn-clean" type="button" onclick="window.MawarCart.toggle(false)">✕</button>
    </div>

    <div id="cartDrawerItems" class="muted cart-items-clean"></div>

    <div class="cart-panel-actions">
      <a href="/checkout" class="checkout-link cart-checkout-btn">Checkout</a>
      <button class="iconbtn clear-cart-btn" type="button" onclick="window.MawarCart.clear()">Kosongkan</button>
    </div>
  </aside>
</div>

<style>
  .top-strip-clean{
    position:sticky;
    top:0;
    z-index:1003;
    background:rgba(12,12,16,.92);
    border-bottom:1px solid rgba(255,255,255,.06);
    backdrop-filter:blur(12px);
    -webkit-backdrop-filter:blur(12px);
  }

  .top-strip-clean .strip-row{
    min-height:40px;
    display:flex;
    justify-content:space-between;
    align-items:center;
    gap:10px;
    padding:8px 0;
  }

  .top-strip-clean .strip-left{
    color:rgba(255,255,255,.62);
    font-size:11px;
    letter-spacing:.14em;
    text-transform:uppercase;
  }

  .top-strip-clean .strip-right{
    display:flex;
    align-items:center;
    gap:8px;
    flex-wrap:wrap;
  }

  .top-strip-clean .cartlink{
    display:inline-flex;
    align-items:center;
    min-height:32px;
    padding:0 12px;
    border-radius:999px;
    border:1px solid rgba(255,255,255,.08);
    background:rgba(255,255,255,.03);
    color:#f5f1e8;
    cursor:pointer;
    font-size:11px;
    letter-spacing:.08em;
    text-transform:uppercase;
  }

  .top-strip-clean .checkout-link{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    min-height:32px;
    padding:0 14px;
    border-radius:999px;
    border:1px solid rgba(231,192,107,.28);
    background:transparent;
    color:#ffe2a3;
    font-size:11px;
    font-weight:700;
    letter-spacing:.08em;
    text-transform:uppercase;
  }

  .header-clean{
    position:sticky;
    top:40px;
    z-index:1002;
    background:rgba(10,10,14,.94);
    border-bottom:1px solid rgba(255,255,255,.06);
    backdrop-filter:blur(14px);
    -webkit-backdrop-filter:blur(14px);
  }

  .row-clean{
    display:grid;
    grid-template-columns:1fr auto 1fr;
    align-items:center;
    gap:12px;
    padding:14px 0;
  }

  .brand-link-clean{
    color:#fff;
    text-decoration:none;
  }

  .brand-text-clean{
    display:inline-block;
    color:#fff4d6;
    font-size:24px;
    font-weight:900;
    letter-spacing:.28em;
    text-transform:uppercase;
  }

  .iconbtn-clean{
    width:40px;
    height:40px;
    border-radius:12px;
    border:1px solid rgba(255,255,255,.08);
    background:rgba(255,255,255,.03);
    color:#fff4d6;
    display:inline-flex;
    align-items:center;
    justify-content:center;
  }

  .nav-clean{
    border-top:1px solid rgba(255,255,255,.05);
  }

  .nav-clean ul{
    list-style:none;
    margin:0;
    padding:10px 18px;
    display:flex;
    justify-content:center;
    align-items:center;
    gap:10px;
    flex-wrap:wrap;
  }

  .nav-clean a{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    min-height:34px;
    padding:0 14px;
    border-radius:999px;
    color:rgba(255,255,255,.82);
    font-size:11px;
    letter-spacing:.10em;
    text-transform:uppercase;
    border:1px solid transparent;
    transition:.2s ease;
  }

  .nav-clean a:hover{
    color:#fff4d6;
    border-color:rgba(231,192,107,.18);
    background:rgba(255,255,255,.03);
  }

  .search-overlay-clean{
    position:fixed;
    inset:0;
    z-index:2100;
    display:none;
    align-items:flex-start;
    justify-content:center;
    padding:60px 16px 20px;
    background:rgba(0,0,0,.55);
    backdrop-filter:blur(8px);
  }

  .searchbar-clean{
    width:min(720px,100%);
    padding:18px;
    border-radius:20px;
    border:1px solid rgba(255,255,255,.08);
    background:rgba(14,14,18,.96);
    box-shadow:0 20px 60px rgba(0,0,0,.3);
  }

  .searchhint{
    color:rgba(255,255,255,.58);
    font-size:11px;
    letter-spacing:.14em;
    text-transform:uppercase;
    margin-bottom:10px;
  }

  .searchrow-clean{
    display:flex;
    align-items:center;
    gap:10px;
    min-height:52px;
    padding:0 14px;
    border-radius:16px;
    border:1px solid rgba(255,255,255,.08);
    background:rgba(255,255,255,.03);
  }

  .searchrow-clean input{
    flex:1;
    border:none;
    outline:none;
    background:transparent;
    color:#fff;
    font-size:14px;
  }

  .closebtn-clean{
    margin-top:12px;
    min-height:38px;
    padding:0 14px;
    border-radius:12px;
    border:1px solid rgba(255,255,255,.08);
    background:rgba(255,255,255,.03);
    color:#fff4d6;
    cursor:pointer;
  }

  .cartDrawerClean .cart-backdrop{
    position:absolute;
    inset:0;
    background:rgba(0,0,0,.55);
  }

  .cartDrawerClean .cart-panel{
    position:absolute;
    top:0;
    right:0;
    width:min(420px,100%);
    height:100%;
    padding:20px;
    overflow:auto;
    background:rgba(14,14,18,.98);
    border-left:1px solid rgba(255,255,255,.08);
  }

  .cart-panel-head{
    display:flex;
    justify-content:space-between;
    align-items:center;
    gap:10px;
    margin-bottom:14px;
  }

  .cart-panel-head h3{
    margin:0;
    color:#fff4d6;
    font-size:22px;
    letter-spacing:.08em;
    text-transform:uppercase;
  }

  .cart-panel-actions{
    margin-top:16px;
    display:flex;
    gap:10px;
    flex-wrap:wrap;
  }

  .cart-checkout-btn{
    min-height:40px;
    padding:0 16px;
  }

  .clear-cart-btn{
    width:auto;
    min-height:40px;
    padding:0 14px;
  }

  @media (max-width:900px){
    .brand-text-clean{
      font-size:20px;
      letter-spacing:.20em;
    }
  }

  @media (max-width:600px){
    .top-strip-clean .strip-row{
      justify-content:center;
    }

    .top-strip-clean .strip-left{
      width:100%;
      text-align:center;
      font-size:10px;
    }

    .top-strip-clean .strip-right{
      justify-content:center;
    }

    .header-clean{
      top:40px;
    }

    .brand-text-clean{
      font-size:15px;
      letter-spacing:.16em;
    }

    .iconbtn-clean{
      width:38px;
      height:38px;
    }

    .nav-clean{
      overflow-x:auto;
      scrollbar-width:none;
    }

    .nav-clean::-webkit-scrollbar{
      display:none;
    }

    .nav-clean ul{
      width:max-content;
      min-width:100%;
      flex-wrap:nowrap;
      justify-content:flex-start;
      padding:10px 14px;
    }

    .nav-clean a{
      white-space:nowrap;
      font-size:9px;
      padding:0 12px;
    }

    .searchbar-clean{
      padding:16px;
    }

    .cartDrawerClean .cart-panel{
      width:100%;
      padding:18px 16px;
    }
  }
</style>
